create dictionary
  - comp address
  - symmetric key

secure send (AP)
  - generate key
  - add to comp(n) dict
  - send key every (count / component_cnt) % 5

secure receive (Comp)
  - lookup key from dict

secure receive (comp)
  - receive key every count % 5
  - set symmetric key
